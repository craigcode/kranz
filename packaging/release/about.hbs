Kranz Rust dependency notices
Generated by cargo-about 0.9.2 from the locked CLI dependency graph.
Includes build dependencies and the union of all five release targets;
some listed packages may not be linked into an individual executable.

{{#each licenses}}
================================================================================
{{name}} ({{id}})
Used by:
{{#each used_by}}
- {{crate.name}} {{crate.version}} <{{crate.repository}}>
{{/each}}

{{{text}}}

{{/each}}
