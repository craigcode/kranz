# Mission plan — m-d341a7

**Goal:** Produce docs/gascity-citizenship.md — the grounded plan of record for making kranz a full citizen of Gas City: current-state inventory of the pack, a mechanism-by-mechanism assessment of the installed gc 1.3.2 surface, a staged roadmap with explicit design decisions, and ticket-ready briefs for the first autonomously verifiable steps.

Branch `kranz/mission-m-d341a7` (from `main`). Approved plan of record; the machine-readable twin is [plan.json](plan.json). Live status: `kranz status` or the dashboard.

## Validation contract

Defined before any feature; gates mission completion.

- **[a1]** The plan-of-record document exists at docs/gascity-citizenship.md and contains all six required second-level sections: Current state; Constraints and invariants; Citizenship assessment; Staged roadmap; Design decisions; Ticket-ready briefs. 
  `bash -c 'for h in "## Current state" "## Constraints and invariants" "## Citizenship assessment" "## Staged roadmap" "## Design decisions" "## Ticket-ready briefs"; do grep -qF "$h" docs/gascity-citizenship.md || { echo "missing section: $h"; exit 1; }; done'`
- **[a2]** The document states the exact gc version it was grounded against, matching the gc binary installed on this machine at validation time. 
  `bash -c 'grep -qF "gc $(gc version)" docs/gascity-citizenship.md'`
- **[a3]** Every one of the twenty core Gas City mechanisms (order, hook, sling, mail, handoff, nudge, events, formula, convoy, converge, agent, prime, session, status, dashboard, doctor, pack, lint, mcp, skill) is assessed under a heading of the exact form '### gc <name> — <verdict>' with verdict adopt, defer, or reject. 
  `bash -c 'for m in order hook sling mail handoff nudge events formula convoy converge agent prime session status dashboard doctor pack lint mcp skill; do grep -qE "^### gc $m — (adopt|defer|reject)" docs/gascity-citizenship.md || { echo "missing verdict heading for: $m"; exit 1; }; done'`
- **[a4]** The mission changes documentation only: no file outside docs/ is modified relative to the pinned base commit (kranz bookkeeping under .kranz/ excluded). 
  `bash -c 'git rev-parse -q --verify "$KRANZ_BASE_SHA^{commit}" >/dev/null && ! git diff --name-only "$KRANZ_BASE_SHA" | grep -vE "^(\.kranz/|docs/)" | grep -q .'`
- **[a5]** The Ticket-ready briefs section contains at least three briefs, each introduced by a heading of the exact form '#### Brief N: <title>'. 
  `bash -c '[ "$(grep -cE "^#### Brief [0-9]+:" docs/gascity-citizenship.md)" -ge 3 ]'`
- **[a6]** Design decisions D1 through D6 are each present under a heading of the exact form '### Dn — <title>', and the document carries at least six explicit '**Recommendation:**' lines. 
  `bash -c 'for d in 1 2 3 4 5 6; do grep -qE "^### D$d — " docs/gascity-citizenship.md || { echo "missing decision D$d"; exit 1; }; done && [ "$(grep -c "^\*\*Recommendation:\*\*" docs/gascity-citizenship.md)" -ge 6 ]'`
- **[a7]** The staged roadmap defines at least four stages under '### Stage N' headings, and at least four explicit '**Trigger:**' lines state when each stage becomes worth doing. 
  `bash -c '[ "$(grep -cE "^### Stage [0-9]" docs/gascity-citizenship.md)" -ge 4 ] && [ "$(grep -c "^\*\*Trigger:\*\*" docs/gascity-citizenship.md)" -ge 4 ]'`
- **[a8]** The plan of record is discoverable: docs/gascity.md and the docs list in docs/what-is-kranz.md both reference gascity-citizenship.md. 
  `bash -c 'grep -q "gascity-citizenship.md" docs/gascity.md && grep -q "gascity-citizenship.md" docs/what-is-kranz.md'`
- **[a9]** Every capability, command, flag, or behavior the document attributes to Gas City is real in the installed gc 1.3.2 (spot-verifiable via read-only 'gc help' and 'gc <cmd> --help'), and every claim about kranz's current integration matches the repository (packaging/gascity/*, docs/gascity.md, the kranz exec exit-code contract) — nothing is invented. *(agent judgement)*
- **[a10]** The plan is honest and internally consistent: every mechanism verdict carries a reason; defer verdicts name their trigger; roadmap stages flag human-gated work with '**Human-gated:**' and no ticket-ready brief depends on a human-gated prerequisite; D1–D6 recommendations do not contradict the mechanism verdicts; each brief is self-sufficient by the bead-authoring rules (goal carried by the title line, constraints in context, testable acceptance). *(agent judgement)*

## Milestone 1 — Grounded inventory — current state, constraints, and the full gc 1.3.2 citizenship assessment

### 1.1 Create the plan-of-record doc: skeleton, current-state inventory, constraints

CONTEXT. This repo is kranz, a Rust mission harness for the claude CLI. packaging/gascity/ holds a stub-verified integration 'pack' for Gas City — an orchestration-builder CLI (`gc`, version 1.3.2, installed at /opt/homebrew/bin/gc). The spike that produced the pack is documented in docs/gascity.md. This mission writes a plan-of-record document for making kranz a 'full citizen' of Gas City; your feature creates that document and writes its first two sections. This is a DOCUMENTATION mission: modify ONLY docs/gascity-citizenship.md in this feature — no code, no packaging/ changes, no Cargo files.

READ FIRST: docs/gascity.md (all of it, especially the six numbered lessons and the Verdict section); packaging/gascity/pack.toml, README.md, bin/kranz-dispatch, bin/kranz-city-worker, bin/kranz-run-bead, agents/kranz-worker/agent.toml; the exit-code handling in crates/cli/src/exec.rs (read its module docs and search it for the exit codes — do not modify it).

GC USAGE RULES (apply to the whole mission). You may run ONLY these read-only commands: `gc version`, `gc help`, `gc <cmd> --help`, `gc <cmd> --json-schema`, `gc cities`. Anything else is FORBIDDEN — in particular NEVER run `gc init`, `gc register`, `gc start/stop/restart/resume/suspend/reload`, `gc supervisor`, `gc order run`, `gc sling`, `gc handoff`, `gc mail send`, `gc nudge`, or any bd/bead/event mutation: `gc init` registers a machine-wide launchd supervisor and spawns a live, paid claude session (docs/gascity.md lesson 5). No cities are registered on this machine; city-scoped commands will error — that is expected, record it as a constraint, do NOT create a city. No network access.

CREATE docs/gascity-citizenship.md with:
1. Title line `# Kranz × Gas City — citizenship plan of record`, then a preamble stating: date 2026-07-05; that it was grounded against `gc X.Y.Z` where X.Y.Z is the LITERAL output of `gc version` (the contract greps for the exact string 'gc ' + that output, e.g. 'gc 1.3.2'); that it is the plan of record, companion to docs/gascity.md's spike findings; and a short table of contents.
2. Exactly these six second-level headings, in this order: `## Current state`, `## Constraints and invariants`, `## Citizenship assessment`, `## Staged roadmap`, `## Design decisions`, `## Ticket-ready briefs`. Sections you do not write get one italic placeholder line '*Populated by a later feature.*' — later features fill them.
3. FILL `## Current state`: a component-by-component inventory of pack v2, each of the five files above named by path with its role — the fast-path dispatch order (claims ready kranz-labeled beads, spools ticket-shaped briefs, refuses scrutiny-disabled rigs unless KRANZ_ALLOW_UNVALIDATED=1, routes rigs by KRANZ_RIG_DIR or bead-id prefix via `gc rig list --json`); the supervised strictly-serial spool-drain worker (single-instance lock, --once mode); the per-bead runner mapping kranz exit codes to City state including the `gc mail send human --notify` escalation on exit 2; agent.toml declaring kranz-worker a long_running non-LLM supervised session (intended wiring, never validated against a live supervisor). State verification status honestly: stub-verified only; `gc cities` currently reports no registered cities (run it read-only and quote its output). Document the exit-code contract as the integration API — 0 complete/close, 2 blocked/escalate, 3 underspecified/refine, 1 failed/reopen — confirmed against crates/cli/src/exec.rs and docs/gascity.md's field mapping (bead title → ## Goal, description → ## Context, acceptance_criteria → ## Acceptance hints). Close with the spike verdict, cited to docs/gascity.md: capability-neutral for a single operator today; earns its keep in three futures — heterogeneous fleets, distribution as a published pack, cross-machine execution.
4. FILL `## Constraints and invariants`: distill each of docs/gascity.md's six numbered lessons into a standing constraint (order-exec deadline → orders dispatch and return; the bead title is the prompt; scrutiny stays ON for autonomous dispatch — validators-off configs are trusted-brief-only; bd flag dialects; gc init side effects — machine-wide supervisor, live paid mayor session, orphanable tmux servers; merge/push policy belongs to the dispatching side — kranz exec leaves work on the mission branch). Then add the invariants this plan holds fixed: kranz remains fully usable standalone (the pack is optional); missions get no network access (publishing and gastownhall.ai are human-gated); kranz missions and workers never run state-mutating gc commands; the exit-code contract is the stable API.

SELF-CHECK before reporting — run these verbatim and make them pass:
bash -c 'for h in "## Current state" "## Constraints and invariants" "## Citizenship assessment" "## Staged roadmap" "## Design decisions" "## Ticket-ready briefs"; do grep -qF "$h" docs/gascity-citizenship.md || { echo "missing: $h"; exit 1; }; done'
bash -c 'grep -qF "gc $(gc version)" docs/gascity-citizenship.md'

Done when:
- docs/gascity-citizenship.md exists; the six-heading grep loop from the spec passes verbatim, with the six sections in the specified order.
- The preamble contains the literal string 'gc ' followed by the exact output of `gc version` (the a2 contract command passes).
- ## Current state names all five packaging/gascity file paths, all four exit codes with their City effects, and quotes the current `gc cities` output; the exit-code semantics match crates/cli/src/exec.rs.
- ## Constraints and invariants covers all six docs/gascity.md lessons plus the standalone-kranz, scrutiny-on, no-network, and read-only-gc invariants.
- git diff --name-only $KRANZ_BASE_SHA shows no modified file outside docs/ (and .kranz/ bookkeeping); only docs/gascity-citizenship.md was created or edited by this feature.

### 1.2 Citizenship assessment I — work routing and communication mechanisms

CONTEXT. This repo is kranz, a Rust mission harness. docs/gascity-citizenship.md is the in-progress plan of record for integrating kranz as a full citizen of Gas City (`gc` CLI, v1.3.2, on PATH); an earlier feature created it with six sections. READ THE WHOLE DOC FIRST, especially '## Current state' and '## Constraints and invariants', plus docs/gascity.md and packaging/gascity/README.md and bin/kranz-dispatch. This is a DOCUMENTATION mission: modify ONLY docs/gascity-citizenship.md.

GC USAGE RULES: you may run ONLY read-only commands — `gc version`, `gc help`, `gc <cmd> --help`, `gc <cmd> --json-schema`, `gc cities`. NEVER run `gc init` (registers a machine-wide launchd supervisor and spawns a live paid claude session), register/start/stop/restart/resume/suspend/reload/supervisor, `gc order run`, sling, handoff, mail send, nudge, or any bd/bead/event mutation. No cities are registered; city-scoped commands erroring is expected and worth recording. No network.

TASK. Under `## Citizenship assessment` (replace its placeholder): first write an intro paragraph defining citizenship — kranz participating in City machinery natively instead of sitting behind a cooldown-polling order and a private spool directory — and stating the method: every assessment below is grounded in `gc <cmd> --help` probing, verdict-first, honest (rejecting is as valuable as adopting). Then add ONE subsection PER mechanism for exactly these ten, each headed EXACTLY `### gc <name> — <verdict>` (em dash —, verdict one of adopt/defer/reject): order, hook, sling, mail, handoff, nudge, events, formula, convoy, converge.

Each subsection: (1) one or two lines on what the mechanism is, taken from its --help (quote sparingly, never invent flags); (2) three to eight lines on what citizenship would concretely mean for kranz, tied to the existing pack — e.g. order: event-driven triggers vs the current cooldown polling (probe `gc order --help` for trigger kinds); hook: whether kranz-city-worker could consume City-routed work natively instead of the private spool; sling: kranz as a routable work target; mail: two-way — escalations already flow out on exit 2, could inbound City mail feed kranz's guidance inbox for blocked missions; handoff/nudge: whether they apply to a non-LLM supervised worker; events: surfacing kranz mission progress (mission started/blocked/complete) as City events; formula: exposing 'validated kranz mission' as a composable formula; convoy: multi-bead kranz work as a graph; converge: whether kranz's own fix cycles subsume it; (3) a verdict with an explicit reason tied to the Constraints section and the three futures (fleets, distribution, cross-machine). Verdict discipline: adopt = worth a roadmap stage on current evidence; defer = real value behind an explicit named trigger (name it in the text); reject = duplicates kranz's own rails or violates an invariant (say which). You MAY add extra mechanisms beyond the ten (e.g. graph, beads) in the same heading format — but only ones that exist in `gc help`.

SELF-CHECK before reporting — run verbatim and make it pass:
bash -c 'for m in order hook sling mail handoff nudge events formula convoy converge; do grep -qE "^### gc $m — (adopt|defer|reject)" docs/gascity-citizenship.md || { echo "missing: $m"; exit 1; }; done'

Done when:
- All ten headings '### gc <name> — <verdict>' (order, hook, sling, mail, handoff, nudge, events, formula, convoy, converge) are present with verdict adopt, defer, or reject; the self-check grep loop passes verbatim.
- Every subsection's description of the mechanism is consistent with `gc <name> --help` output — no invented flags, subcommands, or behaviors.
- Every verdict states an explicit reason; every defer verdict names its trigger condition in the subsection text.
- The section's intro paragraph defines citizenship and the assessment method as specified.
- Only docs/gascity-citizenship.md is modified; no state-mutating gc command was needed or run (the write-up cites only read-only probing).

### 1.3 Citizenship assessment II — identity, observability, and packaging mechanisms

CONTEXT. This repo is kranz, a Rust mission harness. docs/gascity-citizenship.md is the in-progress plan of record for integrating kranz as a full citizen of Gas City (`gc` CLI, v1.3.2, on PATH). Earlier features wrote its Current state, Constraints, and the first ten mechanism assessments under '## Citizenship assessment'. READ THE WHOLE DOC FIRST (match the established subsection style exactly), plus docs/gascity.md, packaging/gascity/agents/kranz-worker/agent.toml, and packaging/gascity/pack.toml. This is a DOCUMENTATION mission: modify ONLY docs/gascity-citizenship.md.

GC USAGE RULES: read-only ONLY — `gc version`, `gc help`, `gc <cmd> --help`, `gc <cmd> --json-schema`, `gc cities`. NEVER run `gc init` (machine-wide launchd supervisor + live paid claude session), register/start/stop/restart/resume/suspend/reload/supervisor, order run, sling, handoff, mail send, nudge, or any bd/bead/event mutation. ONE exception, because it is a validator by design: you may attempt `gc lint` against packaging/gascity (consult `gc lint --help` for usage). If it runs read-only, record its actual verdict on the pack verbatim in the gc lint subsection; if it demands a live city or attempts any mutation, abort the attempt and record that limitation instead. No network.

TASK. Extend `## Citizenship assessment` with ONE subsection PER mechanism for exactly these ten, each headed EXACTLY `### gc <name> — <verdict>` (em dash —, verdict one of adopt/defer/reject): agent, prime, session, status, dashboard, doctor, pack, lint, mcp, skill.

Each subsection: (1) one or two lines on what it is, from --help; (2) three to eight lines on what citizenship would mean for kranz concretely — agent/prime: what the existing agents/kranz-worker/agent.toml (long_running, non-LLM) does and does not get from agent-hood; whether a behavioral prompt (`gc prime`) ever applies to a non-LLM worker, and whether a City-visible LLM session for kranz would violate the no-resident-LLM stance (flag this for decision D1, do not decide it here); session: City interactive chat sessions vs kranz's own web/Slack/CLI surfaces; status/dashboard: kranz missions appearing in `gc status` and the City dashboard vs kranz's own dashboard — what a citizen should report upward; doctor: a kranz health contribution (spool depth, mission-lock liveness probe); pack: the kranz pack as a distributable artifact via remote pack sources — the distribution future; lint: whether packaging/gascity lints clean today (see the exception above — record the real outcome); mcp: whether kranz's REST surface could project as MCP config for City agents; skill: skills visibility for kranz-shaped work; (3) a verdict with an explicit reason tied to the Constraints section and the three futures (fleets, distribution, cross-machine); defer verdicts name their trigger; reject verdicts say which invariant or duplication drives the rejection. You MAY add extras (import, prompt, analyze, wait, github, build-image, runtime, trace) in the same format — only mechanisms that exist in `gc help`.

FINISH the section with a short summary paragraph tallying the verdicts (N adopt / N defer / N reject across all assessed mechanisms — count the actual subsections, including any extras and the ten from the earlier feature).

SELF-CHECK before reporting — run verbatim and make it pass:
bash -c 'for m in agent prime session status dashboard doctor pack lint mcp skill; do grep -qE "^### gc $m — (adopt|defer|reject)" docs/gascity-citizenship.md || { echo "missing: $m"; exit 1; }; done'

Done when:
- All ten headings '### gc <name> — <verdict>' (agent, prime, session, status, dashboard, doctor, pack, lint, mcp, skill) are present with verdict adopt, defer, or reject; the self-check grep loop passes verbatim.
- The gc lint subsection records the actual outcome of attempting `gc lint` against packaging/gascity — either its real verdict on the pack or the concrete reason the attempt could not run read-only.
- Every subsection is consistent with `gc <name> --help` output (no invented capabilities); every verdict has an explicit reason; defer verdicts name their trigger.
- A summary paragraph closes the section with adopt/defer/reject tallies that match the actual count of '### gc ' subsections in the document.
- Only docs/gascity-citizenship.md is modified.


## Milestone 2 — Plan of record — staged roadmap, design decisions, and ticket-ready briefs

### 2.1 Staged citizenship roadmap and design decisions D1–D6

CONTEXT. This repo is kranz, a Rust mission harness. docs/gascity-citizenship.md is the in-progress plan of record for making kranz a full citizen of Gas City (`gc` CLI, v1.3.2, on PATH). Earlier features completed '## Current state', '## Constraints and invariants', and a verdict-per-mechanism '## Citizenship assessment' (headings '### gc <name> — adopt|defer|reject'). READ THE WHOLE DOCUMENT FIRST — your sections must build on those verdicts, and must not contradict them (never anchor a stage on a rejected mechanism). Also read docs/gascity.md (deviations and verdict). This is a DOCUMENTATION mission: modify ONLY docs/gascity-citizenship.md. gc usage: read-only only (`gc version/help/<cmd> --help/--json-schema/cities`); never any state-mutating gc command; no network.

FILL `## Staged roadmap` (replace its placeholder). At least FOUR stages, each headed `### Stage N — <name>` (em dash), ordered from today's posture to full citizenship. Every stage MUST carry: its scope (which adopted/deferred mechanisms it lands, referencing the assessment subsections); work items sized as kranz missions (the repo's history prices single-feature briefs at roughly $30–50); a verification story that separates what is autonomously verifiable at stub level (the norm — no live city exists on this machine) from what needs a human, flagging each human-dependent item on its own line starting exactly `**Human-gated:**` (live-city operations, supervised `kranz serve` restarts, pack publishing to gastownhall, anything networked); and a line starting exactly `**Trigger:**` stating the condition under which the stage is worth doing at all. Stages may and should conclude 'not until the trigger fires' where honest — this plan inherits docs/gascity.md's reality-check ethos: it is an assessment, not advocacy. Suggested seed progression to refine against the actual verdicts (NOT binding): Stage 0 — today, pack v2 parked and stub-verified; Stage 1 — live-city validation of the existing pack in a disposable city (spell out exact setup and teardown commands a HUMAN would run, including reaping the orphanable tmux server per docs/gascity.md lesson 5); Stage 2 — event-driven dispatch and two-way mail (guidance backflow); Stage 3 — City-visible progress and health (events/status/doctor; note kranz already exports OTEL — see docs/otel.md — when weighing a second telemetry path); Stage 4 — distribution (lint-clean published pack); Stage 5 — fleets and cross-machine (explicitly speculative).

FILL `## Design decisions` (replace its placeholder) with EXACTLY six decisions headed `### D1 — <title>` through `### D6 — <title>` (em dash; you may add D7+ if genuinely needed): D1 the opacity boundary — hold the one-opaque-agent stance (no resident LLM sessions City-side, City's richer machinery bypassed) or open kranz internals to City where the assessment justifies it; D2 the dispatch model — cooldown-polling order vs event-driven order vs native `gc hook` routing; D3 guidance backflow — City mail feeding kranz's guidance inbox for blocked missions vs keeping steering exclusively on kranz-native surfaces (web/Slack/CLI); D4 supervision and health — the current kranz-city-worker under the supervisor's health patrol vs `kranz serve` itself as the supervised City service with a lock-probe health check; D5 the private spool vs kranz-native queue — docs/gascity.md deviation 1 names a long-lived `kranz work` dispatcher as the production path; D6 verification strategy for city-coupled behavior — design of a disposable test-city fixture (lifecycle, cost, side-effect risks, teardown) vs staying stub-only. EACH decision: the realistic options, then a line starting exactly `**Recommendation:**` naming the chosen option, why (tied to constraints and verdicts), and what it forecloses.

SELF-CHECK before reporting — run verbatim and make all pass:
bash -c '[ "$(grep -cE "^### Stage [0-9]" docs/gascity-citizenship.md)" -ge 4 ] && [ "$(grep -c "^\*\*Trigger:\*\*" docs/gascity-citizenship.md)" -ge 4 ]'
bash -c 'for d in 1 2 3 4 5 6; do grep -qE "^### D$d — " docs/gascity-citizenship.md || { echo "missing D$d"; exit 1; }; done && [ "$(grep -c "^\*\*Recommendation:\*\*" docs/gascity-citizenship.md)" -ge 6 ]'

Done when:
- At least four '### Stage N — ' headings exist and every stage has a '**Trigger:**' line; the stage self-check command passes verbatim.
- Headings '### D1 — ' through '### D6 — ' all exist, each decision lists options and has a '**Recommendation:**' line; the decision self-check command passes verbatim.
- Every human-dependent work item in the roadmap is flagged on a '**Human-gated:**' line (live-city ops, serve restarts, publishing, anything networked).
- No stage or recommendation builds on a mechanism the assessment rejected, and recommendations cite the constraints/verdicts they rest on.
- Stage 1-style live-city validation spells out human-run setup and teardown including tmux-server reaping per docs/gascity.md lesson 5; only docs/gascity-citizenship.md is modified.

### 2.2 Ticket-ready briefs, doc stitching, and full contract self-verification

CONTEXT. This repo is kranz, a Rust mission harness. docs/gascity-citizenship.md is the nearly complete plan of record for kranz's Gas City citizenship; every section except '## Ticket-ready briefs' is written (current state, constraints, per-mechanism verdicts, staged roadmap with **Human-gated:**/**Trigger:** flags, decisions D1–D6). READ THE WHOLE DOCUMENT FIRST. This is a DOCUMENTATION mission: modify ONLY docs/gascity-citizenship.md, docs/gascity.md, and docs/what-is-kranz.md. gc usage: read-only only (`gc version/help/<cmd> --help/cities`); never any state-mutating gc command; no network.

1. FILL `## Ticket-ready briefs` (replace its placeholder). Open with two or three lines on use: each brief pastes into kranz's backlog (`kranz ticket`) or a Gas City bead via the pack's field mapping (title → Goal, description → Context, acceptance → Acceptance hints). Then write AT LEAST THREE briefs. Each brief: a heading of the exact form `#### Brief N: <imperative title>` (N = 1, 2, 3, …) whose title alone carries the goal (docs/gascity.md lesson 2: the title is the prompt), followed by exactly three bold-labelled parts on their own paragraphs — `**Goal:**` one self-sufficient imperative statement of the outcome; `**Context:**` the constraints a fresh worker needs (file paths, invariants from the doc's constraints section, the read-only-gc rule where relevant, no new dependencies unless stated); `**Acceptance:**` concrete testable outcomes — and any acceptance that names a specific test MUST use a passed-count guard, e.g. piping through grep -qE 'result: ok\. [1-9][0-9]* passed', never a bare cargo-test name filter (a zero-match filter exits 0 — repo lesson). Briefs must map one-to-one onto the roadmap's EARLIEST work items that are autonomously verifiable — never onto anything flagged **Human-gated:**. Derive candidates from the roadmap actually in the document; likely shapes include replacing the pack's private spool with kranz-native queue dispatch (roadmap decision D5), an event-triggered order variant proven against a stub, and a mail-to-guidance bridge proven against a stub — but follow the doc, not this list.

2. STITCH pointers (minimal edits): in docs/gascity.md, extend the Status line at the top to add: plan of record: [gascity-citizenship.md](gascity-citizenship.md) — change nothing else in that file; in docs/what-is-kranz.md, the docs list near line 170 (the block listing docs/design.md, docs/protocol.md, docs/gascity.md, docs/otel.md) gains a matching entry line for docs/gascity-citizenship.md — 'the Gas City citizenship plan of record' — beside the existing gascity.md entry, same list style.

3. COHERENCE PASS over the whole citizenship doc: the preamble TOC matches the six sections; the assessment summary tallies match the actual subsection count; heading formats are uniform (em dashes, exact '### gc <name> — verdict' shapes). Then RUN THE FULL CONTRACT verbatim and fix any drift until every command passes:
bash -c 'for h in "## Current state" "## Constraints and invariants" "## Citizenship assessment" "## Staged roadmap" "## Design decisions" "## Ticket-ready briefs"; do grep -qF "$h" docs/gascity-citizenship.md || exit 1; done'
bash -c 'grep -qF "gc $(gc version)" docs/gascity-citizenship.md'
bash -c 'for m in order hook sling mail handoff nudge events formula convoy converge agent prime session status dashboard doctor pack lint mcp skill; do grep -qE "^### gc $m — (adopt|defer|reject)" docs/gascity-citizenship.md || exit 1; done'
bash -c '[ "$(grep -cE "^#### Brief [0-9]+:" docs/gascity-citizenship.md)" -ge 3 ]'
bash -c 'for d in 1 2 3 4 5 6; do grep -qE "^### D$d — " docs/gascity-citizenship.md || exit 1; done && [ "$(grep -c "^\*\*Recommendation:\*\*" docs/gascity-citizenship.md)" -ge 6 ]'
bash -c '[ "$(grep -cE "^### Stage [0-9]" docs/gascity-citizenship.md)" -ge 4 ] && [ "$(grep -c "^\*\*Trigger:\*\*" docs/gascity-citizenship.md)" -ge 4 ]'
bash -c 'grep -q "gascity-citizenship.md" docs/gascity.md && grep -q "gascity-citizenship.md" docs/what-is-kranz.md'
bash -c 'git rev-parse -q --verify "$KRANZ_BASE_SHA^{commit}" >/dev/null && ! git diff --name-only "$KRANZ_BASE_SHA" | grep -vE "^(\.kranz/|docs/)" | grep -q .'

Done when:
- At least three '#### Brief N:' headings exist and the brief-count self-check passes verbatim; every brief contains exactly the three bold labels **Goal:**, **Context:**, **Acceptance:**.
- Each brief is self-sufficient (a fresh worker needs nothing else), its title alone carries the goal, and any named-test acceptance uses a passed-count guard rather than a bare test-name filter.
- No brief implements an item flagged **Human-gated:** in the roadmap; each brief maps to an identifiable early roadmap work item.
- docs/gascity.md's status area and docs/what-is-kranz.md's docs list both reference gascity-citizenship.md, with no other changes to those files.
- All eight contract commands listed in the spec pass when run verbatim from the repo root, and the mission diff vs $KRANZ_BASE_SHA touches only docs/ (plus .kranz/ bookkeeping).

