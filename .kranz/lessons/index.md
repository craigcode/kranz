- m-8b7db0.md · When a validation-contract `command` asserts that a specific named test passes, never use a bare `cargo test -p <crate> <namefilter>` — a f…
- m-45624a.md · A "no unrelated surfaces" contract command that diffs against `$KRANZ_BASE_SHA` must exclude Kranz's own bookkeeping paths, or it fails on …
- m-d341a7.md · Milestone validation in this repo runs the ENTIRE mission contract at every milestone gate, not just the assertions that milestone can sati…
- m-468277.md · When a validation-contract `command` targets the CLI crate, use `cargo test -p kranz` (not `-p kranz-cli`): the CLI crate at crates/cli is …
