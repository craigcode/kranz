Verify dependency assumptions against the actual mission base at plan time — do not trust a ticket's "not yet landed" framing. Kranz missio…

Verify dependency assumptions against the actual mission base at plan time — do not trust a ticket's "not yet landed" framing. Kranz missions branch off prior mission branches (this one branched from kranz/mission-m-468277), so the base already carries that sibling's merged work. Here the Approved-vs-Executing ticket had already landed: MissionStatus::Approved existed in crates/engine/src/types.rs + reducer.rs (plan.approved→Approved→Running) and in apps/dashboard/src/lib/types.ts. Because the plan was written on the stale "not landed" premise, its predicate spec and contract assertions (a3/a4) were inverted from reality, forcing a respawn and two waived findings. Before writing any contract command or feature spec that hinges on whether a dependency exists, grep the actual base (types.rs/reducer.rs/types.ts) to confirm — and phrase the contract around what's really there.