NONE is tempting since this ran clean, but the preflight surfaced one durable, contract-shaping trap worth carrying forward.

`When a validation-contract command assertion runs `cargo test` to prove a suite passes, pair it with an anti-vacuity guard (e.g. require output matching `test result: ok. [1-9]`), because a bare `cargo test` exits 0 even when a filter or cfg-gate selects zero tests — so the assertion would pass vacuously without ever exercising the target paths.`