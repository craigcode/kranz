---
title: The pipeline view: one list, stage chips, one-tap actions, ticket capture
priority: 2
schedule: once
blocked-by: [pipeline-1-stage-artifacts]
---

## Goal
One screen, one row per work item, rendering the nine-stage model from docs/scoping/pipeline-view.md: stage chips, inline artifacts (plan + estimate at Reviewable, report + diff + UNMERGED badge at Delivered), and exactly one obvious action per row — Draft, Queue, Reshape, Merge-link, Iterate — per the simple-lists/easy-buttons principle. Includes the D-A verb copy, a dashboard new-ticket form, and Slack /kranz ticket new via the modal pattern.

## Context
Design of record: docs/scoping/pipeline-view.md (stage model, decided
D-B/D-C/D-D, design principles: simple lists / easy buttons / iterate
always on offer). Read it in full before planning; do not contradict a
decided section. D-A verb copy: Approve reserved for plan approval,
Queue for ticket-queueing, CONFIRMED (D-A decided).

## Scoping answers

## Acceptance hints
