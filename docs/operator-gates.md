# Operator Gates

Human-only gates surfaced by `/kranz todo`. Keep this list version-controlled;
the Slack bridge reads these bullets instead of hardcoding roadmap gates.

- [x] pre-public history scrub
- [ ] repository visibility changed to public
- [ ] M6 live deploy
- [x] M2.9 live Slack validation
- [ ] G2 hardware
