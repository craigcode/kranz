# v0.2.0 cut checklist (operator)

Date started: 2026-08-23. Prep PR: [#19](https://github.com/craigcode/kranz/pull/19)
(`codex/v0.2.0-review-fixes`). Do not tag or publish until every box is true.

## Agent-completed (this pass)

- [x] Version aligned at workspace / Tauri / CHANGELOG (`v0.2.0`)
- [x] `scripts/check-package-licenses.sh` green
- [x] `cargo publish --dry-run -p kranz-engine` green
- [x] Public-tree + public-history audits green on tip **without** operator
  markers (Gitleaks: no leaks). Markers with `REQUIRE=1` still outstanding.
- [x] Knowledge vault `last_verified` bumped for path-drifted notes
- [x] Secret-scan false positives in server/slack review-fixes cleared or waived

## Owner-required (blocking)

1. [ ] Merge PR #19 when CI is green
2. [ ] Provide `KRANZ_PUBLIC_AUDIT_MARKERS_FILE` and re-run both audits with
   `KRANZ_REQUIRE_OPERATOR_MARKERS=1` on the exact merge SHA
3. [ ] Confirm GitHub secret `KRANZ_PUBLIC_AUDIT_MARKERS` matches that file
4. [ ] Flip `craigcode/kranz` to **public**
5. [ ] Anonymous fresh clone → re-run both audits + visitor checks
   (ruleset, Actions allowlist, security features, `SECURITY.md`)
6. [ ] Set Actions var `KRANZ_PUBLIC_RELEASE_ENABLED=true`
7. [ ] Annotated tag `v0.2.0` on that SHA; push tag (human)
8. [ ] Approve protected `release` environment job
9. [ ] Verify GitHub release assets / SHA256SUMS / attestations on clean hosts
10. [ ] `cargo publish` bottom-up: engine → server → slack → kranz
11. [ ] Homebrew tap = follow-on only (not a first-cut blocker)

## Do not

- Publish from the private archive lineage (`Data/kranz` `main` / `archive`)
- Reuse or resurrect the archived `v0.1.0` preview
- Set the release switch before anonymous re-audit passes
